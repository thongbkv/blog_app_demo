<% if @errors %>
  alert("<%= @errors %>")
<% else %>
  $("#comments_<%=@entry.id%>").prepend("<%= escape_javascript(render partial: 'comments/comment', locals: {comment: @comment}) %>");
  $("#entry-<%=@entry.id%>").find(".comment_content").val('');
<% end %>